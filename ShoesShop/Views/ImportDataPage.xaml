<Page
    x:Class="ShoesShop.Views.ImportDataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" Padding="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
            <TextBlock Text="Import Data" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" />

            <Button Content="Select File" Command="{Binding SelectFileCommand}" Width="200" />
            <TextBlock Text="{Binding FilePath}" Width="400" TextWrapping="Wrap" />

            <Button Content="Import User Data" Command="{Binding ImportUserDataCommand}" Width="200" />
            <Button Content="Import Shoes Data" Command="{Binding ImportShoesDataCommand}" Width="200" />
            <TextBlock Text="{Binding StatusMessage}" Width="400" TextWrapping="Wrap" />

            <ProgressRing IsActive="{Binding IsImporting}" Width="50" Height="50" />
        </StackPanel>
    </Grid>
</Page>
